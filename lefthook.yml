pre-commit:
  commands:
    format:
      run: bun run format
      glob: "*.{ts,tsx,js,jsx,json,md}"
      stage_fixed: true
    lint:
      run: bun run lint
      glob: "*.{ts,tsx,js,jsx}"
    type-check:
      run: bun run type-check
      glob: "*.{ts,tsx}"
    test:
      run: bun run test
      glob: "*.{ts,tsx}"

pre-push:
  commands:
    build:
      run: bun run build
      glob: "*.{ts,tsx,js,jsx}"
